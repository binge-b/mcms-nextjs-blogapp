type BookType = {
    id: number;
    title: string;
    content: string;
    thumbnail: { url: string };
    createdAt: string;
    updatedAt: string;
    contentId: string;
};

export type { BookType };